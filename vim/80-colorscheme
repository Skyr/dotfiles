" Number of color in terminal
"set t_Co=16
set t_Co=256

if has('gui_running')
  set background=light
else
  set background=dark
endif
" let g:solarized_termcolors=256
let g:solarized_termtrans=1
colorscheme solarized

" Fix color for vim-gitgutter
highlight clear SignColumn
